apiVersion: v1
kind: Service
metadata:
  name: agent-builder
  namespace: tas-agent-builder
  labels:
    app: agent-builder
spec:
  type: ClusterIP
  ports:
  - port: 8087
    targetPort: 8087
    protocol: TCP
    name: http
  selector:
    app: agent-builder
